# Environment Configuration for Business Dashboard

# Set to 'true' to force using mock data even when Amplify is configured
# This is useful for local development without AWS backend
VITE_USE_MOCK_DATA=false

# AWS Region (set automatically by Amplify, but can be overridden)
# VITE_AWS_REGION=us-east-1

# API Key (set automatically by Amplify sandbox)
# VITE_API_KEY=your-api-key-here
